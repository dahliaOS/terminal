name: terminal
description: The default xterm for dahliaOS

publish_to: 'none'
version: 2.0.0+2

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  xterm: 
    git: https://github.com/dahliaOS/xterm.git
  flutter_pty: ^0.2.0
  flutter_lints: ^2.0.1
  zenit_ui: 
    git: https://github.com/dahliaOS/zenit_ui.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true